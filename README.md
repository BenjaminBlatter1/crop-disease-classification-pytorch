# Crop Disease Classification with PyTorch

Small end-to-end computer vision project:
- PyTorch training pipeline
- Data preprocessing and augmentation
- Evaluation metrics (accuracy, F1)
- ONNX export for deployment

## Project Structure

- `data/`: raw and processed data (not in repo)
- `src/`: training, dataset, model, evaluation, export
- `notebooks/`: exploration and experiments
- `outputs/`: checkpoints, metrics, figures

## Status

Day 1: Project structure and environment setup.

